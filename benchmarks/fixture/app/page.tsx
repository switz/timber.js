export const metadata = { title: { absolute: 'Home' } };

export default function HomePage() {
  return (
    <main>
      <h1>Benchmark Fixture</h1>
      <p>A representative app for measuring build and runtime performance.</p>
    </main>
  );
}
