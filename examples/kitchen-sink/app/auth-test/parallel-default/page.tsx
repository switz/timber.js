export default function ParallelDefaultPage() {
  return (
    <div data-testid="parallel-default-page">
      <h1 data-testid="parallel-default-heading">Parallel Default Slot Test</h1>
      <p>Main page content — always visible</p>
    </div>
  );
}
