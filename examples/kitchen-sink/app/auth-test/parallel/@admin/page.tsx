export default function AdminSlotPage() {
  return (
    <div data-testid="admin-slot-page">
      <h2 data-testid="admin-slot-heading">Admin Panel</h2>
      <p>This content should only show when access is granted.</p>
    </div>
  );
}
