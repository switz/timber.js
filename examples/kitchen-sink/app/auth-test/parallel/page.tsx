export default function ParallelPage() {
  return (
    <div data-testid="parallel-page">
      <h1 data-testid="parallel-heading">Parallel Slot Auth Test</h1>
      <p>This is the main page content — always visible.</p>
    </div>
  );
}
