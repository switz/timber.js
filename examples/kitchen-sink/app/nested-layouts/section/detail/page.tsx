export default function DetailPage() {
  return (
    <div data-testid="detail-page">
      <p className="text-sm text-stone-700">
        Detail page — both the outer and inner layouts stay mounted during this navigation.
      </p>
    </div>
  );
}
