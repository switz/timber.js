export default function SectionIndexPage() {
  return (
    <div data-testid="section-index-page">
      <p className="text-sm text-stone-700">
        Section index — rendered inside both the outer and inner layouts.
      </p>
    </div>
  );
}
