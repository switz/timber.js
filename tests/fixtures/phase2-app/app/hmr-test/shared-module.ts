/**
 * Shared module imported by both server and client components.
 *
 * HMR tests verify that editing this file propagates changes
 * to both the RSC and client environments.
 */
export const SHARED_VALUE = 'original';
