/**
 * Sidebar loading state — shown during slot navigation transitions.
 */
export default function SidebarLoading() {
  return (
    <div data-testid="sidebar-loading">
      <p>Loading sidebar…</p>
    </div>
  );
}
