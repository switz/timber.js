/**
 * Barrel export — re-exports client components from a non-'use client' file.
 * Tests that RSC plugin correctly handles client references through re-exports.
 */
export { ProjectFilter } from '../project-filter';
export { ProjectCounter } from './counter';
