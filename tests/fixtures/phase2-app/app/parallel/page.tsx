/**
 * Parallel routes home page — main content area.
 * Route: /parallel
 */
export default function ParallelHome() {
  return (
    <div data-testid="parallel-home-content">
      <h2>Home Content</h2>
      <p>Main content area — home.</p>
    </div>
  );
}
